# SharePointのExcelからDataverseへの移行と保管庫構築ガイド

本ドキュメントは、SharePoint上の膨大なExcelファイルを、安全で堅牢なデータ保管庫（Vault）としてDataverseへ移行し、Power BIで活用するための手順とノウハウをまとめたものです。

## 1. Dataverseへ移行する目的とメリット

SharePointのExcelを直接Power BIで読み込む従来の運用には、以下のリスクと課題がありました。
* **データ破損リスク**: 誰でもファイルを開けるため、誤操作によるデータの削除や列構造の変更が起きやすい。
* **リンク切れリスク**: SharePointのフォルダ移動やサイト名変更によってファイルパス（URL）が変わると、Power BIの更新が失敗する。
* **パフォーマンスの低下**: 大量のExcelファイルをPower Queryで結合する処理は非常に重く、更新エラーの原因となる。

**Dataverseに移行（固定化）することで得られるメリット:**
1. **堅牢なセキュリティとデータ保護**: リレーショナルデータベースとして機能し、細やかな権限設定が可能なため、誤削除や改ざんを防ぐ「安全な金庫」として機能する。
2. **URL変更の影響を排除**: 環境ごとに固定のエンドポイントを持つため、ファイルの物理的な置き場所に依存しない。
3. **パフォーマンスの向上**: データベースとして最適化されており、Power BIからのデータ取得が高速かつ安定する。

## 2. 移行の基本方針（Power Appsは不要）

* **目的**: データの「同期」ではなく、過去のデータをDataverseに流し込んで「固定化（保管）」すること。
* **ツール**: ユーザー向けの「Power Appsアプリ」を作成する必要はありません。管理者が裏側で**「Power Platform データフロー (Dataflows)」**を使用して、ExcelデータをDataverseテーブルに一方通行で流し込みます。一度流し込めば、元のExcelが消えてもDataverseのデータは保持されます。

## 3. Dataverseテーブルの作成手順

1. **Power Appsポータルにアクセス**: `make.powerapps.com` に管理者としてサインイン。
2. **テーブルの新規作成**: 左メニューの「テーブル」から「新しいテーブル」を作成。
3. **プライマリ名列の設定**: 各行を代表する列（例：伝票番号、レコードIDなど）を設定する。
4. **列（カラム）の追加**: Excelの列構造に合わせて、Dataverse側に列を追加する。この際、適切なデータ型（テキスト、通貨、日付など）を厳格に指定することでデータ品質を担保する。
   * *(注: Copilot機能を利用して、テーブル定義を自動生成することも可能です)*

## 4. Power BIからのDataverse接続とトラブルシューティング

テーブルにデータが格納されたら、Power BIから接続します。

### 4.1 基本的な接続方法
Power BI Desktopの「データを取得」>「Dataverse」を選択し、サインインして対象の環境とテーブルを選択します。

### 4.2 トラブルシューティング

**Q: 環境を開こうとすると、リストの読み込みに非常に時間がかかる**
* **原因**: Dataverse環境内にはシステム管理用の隠しテーブルが数百個存在するため。
* **解決策**: ナビゲーター画面左上の**「検索ボックス」**を利用し、作成したテーブル名（またはその複数形）を直接検索することで即座に表示できます。

**Q: `SQL Server への接続を開けませんでした (provider: Named Pipes Provider, error: 40)` というエラーが出る**
* **原因**: Power BIのDataverseコネクタは裏側でSQL通信（TDSエンドポイント）を使用しますが、セキュリティ上の理由でデフォルトで無効になっているため。
* **解決策**:
    1. Power Platform 管理センター（admin.powerplatform.microsoft.com）にアクセス。
    2. 対象環境の「設定」>「製品」>「機能」を開く。
    3. **「TDS エンドポイント (TDS endpoint)」**を「オン」にして保存。

**Q: 管理センターに自分の環境が表示されず、TDS設定が変更できない**
* **原因**: テナントまたは環境の管理者権限がないため。
* **解決策（ODataフィードによる迂回）**:
    1. `make.powerapps.com` の右上「歯車アイコン」>「開発者リソース」から **「Web API エンドポイント」のURL** をコピーする。
    2. Power BIで「データを取得」> **「OData フィード」** を選択。
    3. コピーしたURLを貼り付け、「組織アカウント」でサインインして接続する。これによりSQL通信（ポート1433）を回避し、HTTPSでデータ取得が可能。

## 5. 環境の作成と権限管理（Dataverse for Teamsの活用）

管理者権限がなく、かつ「環境ごと削除されるリスク」を避けるための現実的なアプローチです。

### 5.1 環境作成の制約
通常のDataverse環境の作成には管理者権限とテナントの容量枠が必要です。IT部門に「保管庫用の専用環境」を依頼するのが最も安全な王道ですが、自力で進める場合は**Dataverse for Teams**を活用します。

### 5.2 既存のTeamsを利用した安全な保管庫の構築
Teamsの新規作成も制限されている場合、自分が所属している既存のTeamsチームの裏側にDataverse環境を構築します。

**構築・権限設定の手順:**
1. **チームの選定**: なるべくメンバーが少なく、関係者のみで構成されている（できれば自分が所有者の）チームを選ぶ。
2. **環境の構築**: Teamsアプリ内で「Power Apps」を検索して開き、「今すぐ開始」から選んだチームを指定してアプリ（環境）を作成する。
3. **【重要】権限の変更**: 既存チームを使うと、デフォルトでチームメンバー全員にDataverseデータの編集・削除権限が付与されてしまいます。
   * Teams内のPower Appsアプリ > 「ビルド」タブ > 「すべて表示」でDataverse管理画面へ。
   * 右上の**「チームのアクセス許可を設定」**を開く。
   * 「メンバー」の権限を「フルアクセス」から **「参照（読むだけ）」** または **「なし」** に変更して保存する。

これにより、既存のTeamsを間借りしながらも、一般メンバーによるデータの改ざん・削除を防ぐ強固なデータ保管庫（Vault）を実現できます。
